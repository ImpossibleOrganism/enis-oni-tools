[package]
name = "oni-tools"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.18", features = ["cargo", "derive"] }
phf = { version = "^0.11", features = ["macros"] }
uom = "^0.36.0"
